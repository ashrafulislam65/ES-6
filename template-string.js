const a = 10;
const b=30;
// const sum = `Result of a ${a} + b ${b} is ${a+b}`
const sum = `Result of a ${a} 
+ b ${b} 
 is ${a+b}`
console.log(sum)
// Advantage : 
// 1.use on multiline string
// 2.string interpolation